{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    {
      "op": "promptAndWaitForAgentMessage",
      "text": "Use the workflow tool exactly once, with args omitted, meta set to { \"name\": \"snapshot-flow\", \"description\": \"one child for the snapshot\" }, and this EXACT script body (copy it verbatim):\nphase('Run')\nconst reply = await agent('Reply with exactly the word WF_CHILD_OK and nothing else.')\ncomplete({ reply })\nThe tool launches this in the background. After its immediate tool result, reply with exactly WORKFLOW_LAUNCHED and stop that turn. When the workflow completion notice later wakes you, reply with exactly WORKFLOW_COMPLETION_ACK and stop. Do not use any other tool.",
      "waitForText": "WORKFLOW_COMPLETION_ACK"
    }
  ]
}
